# Junhui Global Website 新对话交接文档

> 更新时间：2026-09-11（Asia/Shanghai）
>
> 工作分支：`phase-3.7`
>
> 当前已推送代码 HEAD：`d3e4435da63c583a3c46b395d3875ca573801ea1`
>
> 状态：Phase 3.7 继续进行；本机 Demo 与原始内容预览均不等于生产上线

## 1. 新对话第一条消息建议

在新对话中直接发送：

> 请完整阅读当前工作树根目录的 `交接文档-新对话.md` 和 `README.md`，以实际文件、Git 状态和运行状态为准继续维护。工作区是 `D:/Python_Project/New_Official_Website/junhui-global-website/.worktrees/phase-3.6`，分支是 `phase-3.7`。先核对 HEAD、远端、未提交文件、Docker/443 和目标实例；保留所有已有提交、未提交文件、数据卷和私有证据。不要 reset、清库、重导、自动迁移/Seed、自动 commit/push/merge、生产部署或发布 Privacy。原 `junhui-phase37-pilot` 与 `junhui-demo-r2` 是两套不同数据，除非我明确指定，不要互相复制或覆盖。当前最新代码提交是 `d3e4435`。

新对话应先读本文件，不要从旧的 Phase 3.7 Batch01 初始交接重新推断当前状态。旧报告中的 HEAD、测试数量、Basic Auth 和 `junhui.test` 地址都是历史记录，不能直接当作今天的运行结果。

## 2. 项目定位

项目是舟山骏辉塑料机械有限公司的中英文 B2B 官网、后台 CMS 和内容发布平台，面向螺杆、机筒及相关塑化部件业务。

技术栈：

- Website：Nuxt 4、Vue 3、TypeScript，默认 SSR；
- Admin：Nuxt 4、Vue 3、TypeScript，中文运营界面；
- API：FastAPI 模块化单体、SQLAlchemy 2.x、Alembic；
- 基础设施：PostgreSQL、Redis、MinIO、Celery、Nginx、Docker Compose；
- 内容体系：`Master Entity + Translation + Publication + ContentRoute + SeoDocument + Revision + Audit + RBAC`；
- 正式语言路径：`/zh-cn/` 与 `/en/`；
- 正式 canonical 源站：`https://junhuiscrewbarrel.com`。

代码知识图谱在本次交接前核对到 2,136 个节点、11,103 条边，主体仍由 `apps/api`、`apps/admin`、`apps/website` 三部分组成。后续查找函数、路由和调用关系时优先使用项目的 codebase-memory 图工具，配置/字符串和非代码文件再使用 `rg`。

## 3. Git 与工作区

| 项目            | 实际值                                                                              |
| --------------- | ----------------------------------------------------------------------------------- |
| GitHub          | `https://github.com/junhuiscrew/New_Official_Website.git`                           |
| 本机主仓库      | `D:/Python_Project/New_Official_Website/junhui-global-website`                      |
| 当前工作树      | `D:/Python_Project/New_Official_Website/junhui-global-website/.worktrees/phase-3.6` |
| 当前分支        | `phase-3.7`                                                                         |
| 当前已推送 HEAD | `d3e4435da63c583a3c46b395d3875ca573801ea1`                                          |
| 远端跟踪分支    | `origin/phase-3.7`，本次交接前与 HEAD 同步                                          |
| 代码迁移 head   | `20260910_0016_demo_fixed_utility_site_pages`                                       |
| 生产状态        | 未部署、无上线授权                                                                  |

最近关键提交：

```text
d3e4435 feat(demo): complete six-page SEO metadata
2fbcc7d fix(admin): complete media usage and role permission management
5106179 fix(admin): complete Chinese UX R1 closeout
a9cdca5 feat(admin): localize Chinese operations and frontend review
455ff4c fix: close out demo r2.1 ui review
56891e0 feat(demo-r2): complete focused visual closeout
c07a323 feat(demo-r2): build isolated demo website and admin UI
86f7def feat(phase37): implement privacy and website presentation r1
995d1ce feat(phase37): add local domain preview tooling
5b10732 feat(phase37): add products site page SEO
91cea43 feat(phase37): publish remediation code and SEO candidates
afe99c3 feat(phase37): enforce specification lifecycle guard
aed35ce feat(phase37): import batch01 drafts and specification management
```

### 3.1 本轮文档更新后的预期工作树

本次只更新：

- `README.md`：已跟踪文件，状态应为 modified；
- `交接文档-新对话.md`：当前为本地未跟踪文件。

本次开始前还存在以下未跟踪材料，全部属于用户现有本地工作，必须保留：

```text
Junhui-Admin-CN-UX-R1-Functional-Closeout-Evidence-20260910T181356+0800.zip
Junhui-Admin-Chinese-UX-R1-Evidence-20260910T121600+0800.zip
Junhui-Demo-R2.1-Focused-Closeout-Evidence-20260909T152500+0800.zip
Junhui-Demo-SEO-Metadata-R1-Evidence-20260910T234537+0800.zip
artifacts/
docs/content/admin-cn-ux-r1-functional-closeout-report.md
docs/content/demo-seo-metadata-r1-report.md
docs/content/demo-seo-six-pages-readback.json
交接文档-新对话.md
```

不要使用 `git add -A` 或 `git add .`。未来若用户要求提交文档，先逐个检查并显式暂存；数据库 dump、环境文件、私钥、原始 headers/HTML、Cookie、Token 和私有脚本不得进入 GitHub。

## 4. 两套环境必须区分

### 4.1 原始批准内容实例

| 项目            | 值                                          |
| --------------- | ------------------------------------------- |
| Compose project | `junhui-phase37-pilot`                      |
| 中文前台        | `https://junhuiscrewbarrel.com/zh-cn/`      |
| 英文前台        | `https://junhuiscrewbarrel.com/en/`         |
| 后台            | `https://admin.junhuiscrewbarrel.com/`      |
| API             | `https://api.junhuiscrewbarrel.com/api/v1/` |
| 数据            | 原 Phase 3.7 PostgreSQL/Redis/MinIO 卷      |
| 用途            | COPY-V1 与正式候选内容的本机受保护预览      |

它是“真实批准内容的本机隔离实例”，但不是生产环境。前台 Nginx Basic 弹窗已在本机专用网关取消，后台自身登录、RBAC、CSRF、Secure Cookie、私有附件权限仍保留。外层全站 noindex、Sitemap 关闭，只监听 `127.0.0.1:443`。

### 4.2 Demo R2 实例

| 项目            | 值                                                                  |
| --------------- | ------------------------------------------------------------------- |
| Compose project | `junhui-demo-r2`                                                    |
| 中文前台        | `https://demo.junhuiscrewbarrel.com/zh-cn/`                         |
| 英文前台        | `https://demo.junhuiscrewbarrel.com/en/`                            |
| 后台            | `https://admin-demo.junhuiscrewbarrel.com/`                         |
| API             | `https://api-demo.junhuiscrewbarrel.com/api/v1/`                    |
| 数据卷          | `postgres_demo_r2_data`、`redis_demo_r2_data`、`minio_demo_r2_data` |
| Demo batch      | `JH-DEMO-R2-V1`                                                     |
| 用途            | 完整官网内容、前后台 UI 和运营流程演示                              |

Demo 使用独立数据和媒体，不能写回原 phase37。所有演示产品参数、工厂设备、能力、客户、案例、作者、证书、市场和技术结论都不是正式经营事实。

两套环境共享本机 loopback Nginx 边缘入口，但 API、Admin、Website、数据库、Redis 和 MinIO 均不应增加宿主直出端口。`docker-compose.local-domain.demo-edge.yml` 只把共享网关接入 Demo 边缘网络。

## 5. 本次交接时的运行状态

2026-09-11 本次文档更新前进行了只读检查：

- Demo Launcher 确认 Compose 合并后的 loopback isolation 为 PASS；
- `demo.junhuiscrewbarrel.com`、`admin-demo.junhuiscrewbarrel.com`、`api-demo.junhuiscrewbarrel.com` 的 hosts 均指向 `127.0.0.1`；
- Demo 证书 SAN 和本地 CA 校验为 PASS；
- 当前 Codex 会话无权访问 Docker Engine，因此没有取得可信容器列表；
- `127.0.0.1:443` 无监听，直接请求四个 Demo URL 均无法连接。

因此新对话应把运行状态视为 **STOPPED / NOT REACHABLE**，不要因为浏览器仍保留旧标签页就宣称 Demo 正在运行。启动服务属于下一对话的显式操作；本次只写文档，没有启动、迁移或写数据库。

## 6. 启动与停止

### 6.1 完整 Demo

在当前工作树执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\scripts\Junhui-Demo-R2-Launcher.ps1 -Action Status -NoOpen

powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\scripts\Junhui-Demo-R2-Launcher.ps1 -Action Start
```

保留数据停止：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\scripts\Junhui-Demo-R2-Launcher.ps1 -Action Stop -NoOpen
```

桌面快捷方式应为“打开骏辉完整演示站”。如果 hosts/证书尚未配置，才运行 `-Action Setup`；Setup 可能触发一次 Windows UAC，但不应索要系统密码。

### 6.2 原始批准内容预览

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\scripts\Junhui-LocalPreview-Launcher.ps1 -Action Status -NoOpen

powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\scripts\Junhui-LocalPreview-Launcher.ps1 -Action Start
```

桌面快捷方式应为“打开骏辉本地官网”。

### 6.3 重要运行规则

- Windows PowerShell 5.1 必须使用 `*-Launcher.ps1`；直接运行 UTF-8 中文主脚本可能产生解析乱码；
- 当前 runtime 覆盖会将 API 命令固定为 Uvicorn，不自动执行 Alembic 或 Seed；
- 不得为了启动方便运行默认全量 Seed；
- 不得使用 `docker compose down -v`；
- 不要把容器内 `127.0.0.1` 机械替换为 Windows 主机；
- VPN 可保持开启，项目域名通过 hosts、DIRECT 和本地解析精确绕过代理；
- 不关闭 TLS 校验，不使用公网隧道，不修改公共 DNS。

## 7. 凭据与敏感资料

不要在新对话中打印密码值。管理员凭据位置：

- 原始实例：`.env.phase37`
  - `PHASE37_ADMIN_EMAIL`
  - `PHASE37_ADMIN_PASSWORD`
- Demo：`.env.demo-r2`
  - `BOOTSTRAP_ADMIN_EMAIL`
  - `BOOTSTRAP_ADMIN_PASSWORD`

其他敏感变量还包括 PostgreSQL、MinIO、JWT、Refresh Secret、证书私钥和旧 Basic Auth 字段。环境文件已被 `.gitignore` 排除；`data/` 也被排除。`artifacts/` 和根目录 ZIP 并非全部自动忽略，提交前必须人工排除。

禁止泄露：

- 密码、Cookie、Token、CSRF 值、JWT；
- 完整数据库连接字符串；
- MinIO access/secret key 和对象存储键；
- 本地 CA/服务器私钥；
- 私有签名 URL；
- 真实 RFQ、客户资料、图纸、附件和邮箱内容；
- 数据库 dump 与私有恢复脚本。

## 8. 原 phase37 已完成状态

### 8.1 Batch01 COPY-V1

- Company Profile 与 P01/P02/P03 的 16 个中英文字段已经逐字采用 COPY-V1；
- 白名单内 Company、必要分类和三款产品在本机预览内部已通过既有 CMS 生命周期到达 published/Route active；
- 这只是本地受保护预览资格，不是生产发布；
- 三款产品名称、slug、分类、Logo、主图、图注、型号和关系保持批准状态；
- P01/P02/P03 的 ProductSpecValue 仍为 `0/0/0`；
- 七个规格定义保留，F05 英文继续为空；
- “近20年”没有写成数值字段；
- “电镀”没有擅自改成“镀铬”；
- IMG-05 未公开；
- 旧试点 `injection-molding-machine-barrels/nitrided-barrel` 仍是 draft、Route 关闭。

### 8.2 媒体与本地导航修复

- IMG-02/03/04 已按真实文件解码为 `700×700` 并补齐媒体尺寸；
- 文件内容、哈希、媒体 ID、主图关联、水印和 alt 未改变；
- 产品列表、详情和 Gallery 已实际显示；
- Gallery 打开、关闭与焦点返回已验证；
- 语言菜单只把经过正式源站校验的 alternate 转为本站相对路径；
- canonical、hreflang 和 Schema 继续使用正式规范 URL；
- 中文搜索“氮化”可找 P01/P02，“电镀”可找 P03；
- 中文面包屑和本地同源正文链接已修复；
- 空栏目导航后置逻辑和 Sitemap 候选规则已完成，常驻预览 Sitemap 仍为 404。

### 8.3 八组 SEO

以下双语页面共 8 组 title/description 已按 `SEO-CANDIDATE-R1-V1` 保存并验证：

- About；
- Products；
- screws；
- barrels。

Products 使用真实固定页 `SitePage(system_key=products)`、SeoDocument、Translation、Publication、ContentRoute、Revision 和 Audit；没有借用 Company、创建假产品或在 Vue 硬编码。

### 8.4 Privacy P1

技术能力已经实现：

- 固定 Privacy 页面；
- 双语草稿后台；
- 不可变政策版本；
- 双语审核和 current 原子切换；
- 正文 hash、revision 与观察值并发校验；
- 可读但业务 noindex；
- RFQ 短时政策上下文与真实版本/语言/hash 绑定；
- stale、过期、伪造和错语言均拒绝；
- 旧 RFQ 保持 legacy/null，不伪造补签。

原始实例只保存内部双语 draft，没有 Review/Publish，没有 current。公开 `/privacy/` 仍不应返回正式政策；RFQ 正式提交继续禁用。12/6 个月、30 天备份窗口、3/20 天处理目标、730 天等仍只是未批准建议，不是已实施制度。

### 8.5 官网呈现 R1

- 首页 14 个模块均有共享 renderer、后台配置入口和作者完整布局预览；
- 普通首页只使用批准且符合公开门禁的内容；
- 认证预览展示全部位置、缺件原因和管理入口；
- 首页排序、显隐、有限样式、媒体/内容引用、草稿、应用和恢复均走服务端权限、Revision/Audit；
- `/site-overview` 提供全站模块、前后台地址、内容和隐藏原因总览；
- Technologies 与 Contact 已单独核对，没有被笼统视为隐藏栏目。

## 9. Demo R2 已完成状态

### 9.1 数据与媒体（最后一次完整回读口径）

| 项目            | 数量/状态                                |
| --------------- | ---------------------------------------- |
| Demo 内容记录   | 90                                       |
| 产品            | 9                                        |
| 媒体            | 48：43 图、2 WebM、3 PDF                 |
| 媒体校验        | 48/48 checksum verified、48/48 ready     |
| 内容—媒体关联   | 67                                       |
| 业务关系        | 73（初始 72 + 一次真实后台演示新增）     |
| 首页模块        | 14/14 有 renderer、配置和 Demo 内容      |
| Demo RFQ        | 12，四种状态各 3 条，全部为虚构演示      |
| Privacy current | 0                                        |
| 产品规格值      | 45（Demo 数据，不是原 phase37 的 0/0/0） |

Demo 与原始实例最重要的区别：原 P01/P02/P03 参数仍为 0；Demo 的 9 个产品有演示规格值。任何统计前都要先确认数据库目标。

### 9.2 前台与首页

- 采用用户选择的 A 方向：深海军蓝、蓝白高对比、现代工业科技风；
- 首页 14 模块均有有意义 Demo 内容，不再是重复空框；
- About、Contact、产品、分类、材料、工艺、应用、方案、案例、知识、下载、视频和 RFQ 页面可运行；
- 两段 WebM 在浏览器中真实播放，约 12.20 秒和 12.36 秒；
- Demo R2.1 已修复 `/catalog` 占位、规格名称/单位/类型、知识文章回填、后台过大 Hero/过长编辑器、首页/About 布局和产品配图；
- Why Junhui 与 About 优势来自后台内容，不在 Vue 硬编码；
- 特殊应用无媒体时采用完整紧凑文字布局；
- 375px 首页重复曾被确认是长截图拼接伪影，不应在没有复现时重新改页面；
- 产品规格锚点被固定页头遮挡的问题已修复。

### 9.3 中文运营后台

- 标题、字段、按钮、状态、下拉、校验、提示、弹窗、分页和系统页已完成简体中文运营化；
- 英语内容编辑时界面仍为中文，业务正文、slug、Locale、型号、单位和接口枚举不被翻译；
- 案例、知识、FAQ 列表在 1366/1440、100% 缩放下可读；
- 下载页的新建/编辑/加载状态、深色行对比和保存重开已验证；
- RFQ 支持中文状态、北京时间、服务端搜索/筛选/分页；
- 媒体使用位置返回真实具体内容名称、角色和有权限维护入口，不暴露 owner ID、对象键、签名 URL 或私有 RFQ；
- 角色权限页支持中文分组矩阵、搜索、变更对比、确认保存、fresh GET 和重新打开；
- 系统角色只读，无 `role.manage` 时前后端都只读/拒绝；
- 角色写入只使用独立 TEST ONLY 角色验证，没有改系统角色、当前管理员或真实员工权限。

### 9.4 Demo 六页 SEO 收尾（最新提交）

提交 `d3e4435` 完成：

- About zh-CN/en：复用 Company SeoDocument，仅补 description；
- Contact zh-CN/en：新增/复用固定 `SitePage(system_key=contact)`，完成 title、description、canonical、hreflang；
- RFQ zh-CN/en：新增/复用固定 `SitePage(system_key=request-a-quote)`，完成 title、description、canonical、hreflang；
- Contact/RFQ 使用严格固定页白名单和服务端规范 URL；
- RFQ 的 `source_*`、policy token 和查询参数不会进入 canonical 或 Head；
- Admin 增加 `/site-pages/contact` 与 `/site-pages/request-a-quote` 中文编辑入口；
- 保存继续复用 `seo.read/update`、认证、CSRF、Revision/Audit 和正常生命周期；
- 页面首次 SSR 每页只出现一组 title、description、canonical 及 zh-CN/en/x-default hreflang；
- Contact 业务 robots 为 noindex/nofollow，RFQ 为 noindex/follow，外层仍统一 noindex/no-store；
- RFQ 仍不可正式提交，记录数保持 12。

Demo 最后一次 SEO 回读时：

```text
alembic: 20260910_0016
site_pages: contact, home, products, request-a-quote
company_seo: 2
products: 9
product_spec_values: 45
rfqs: 12
privacy_current: 0
revisions: 351
audits: 1414
```

这些是最后一次运行回读，不是当前服务已启动的证明。新对话若要写库，应重新读取目标、revision 和当前状态。

## 10. 主要页面与后台入口

### 10.1 前台

```text
/{lang}/
/{lang}/about/
/{lang}/contact/
/{lang}/products/
/{lang}/products/{category}/
/{lang}/products/{category}/{slug}/
/{lang}/materials/
/{lang}/technologies/
/{lang}/applications/
/{lang}/solutions/
/{lang}/case-studies/
/{lang}/knowledge/
/{lang}/downloads/
/{lang}/request-a-quote/
/{lang}/privacy/
/{lang}/search/
```

### 10.2 后台

```text
/
/site-overview
/homepage
/catalog
/catalog/products
/catalog/specifications
/trust/company
/site-pages/products
/site-pages/contact
/site-pages/request-a-quote
/media
/demo-media
/cases
/knowledge
/faqs
/experts
/downloads
/rfqs
/roles
/users
/privacy
```

原始实例的认证完整布局预览历史地址为：

```text
https://admin.junhuiscrewbarrel.com/preview/zh-cn/
https://admin.junhuiscrewbarrel.com/preview/en/
```

匿名访问作者预览应被拒绝，预览必须 no-store/noindex，不能通过查询参数或 localStorage 绕过。

## 11. 迁移链

当前代码中的迁移顺序：

```text
0001 phase31 core
0002 phase32 foundation
0003 content integrity
0004 structured core
0005 phase33 remediation
0006 authority content
0007 discovery delivery
0008 company trust media
0009 RFQ foundation
0010 phase36 search
0011 site pages (products)
0012 Privacy P1
0013 Privacy P1 hardening
0014 homepage presentation R1
0015 Demo R2 foundation
0016 Demo fixed utility SitePages (contact / request-a-quote)
```

规则：

- 不修改历史 migration；
- 新迁移必须追加在真实最新 head 后；
- 先在独立真实 PostgreSQL 验证 upgrade/downgrade/约束/并发；
- 再给目标实例做私有安全快照；
- 只对获授权实例应用迁移；
- Start 脚本本身不应机械执行迁移或 Seed。

## 12. 最近验证结果

`d3e4435` 对应的 Demo SEO 实施曾实际通过：

| 检查                                              | 结果                 |
| ------------------------------------------------- | -------------------- |
| API 固定页、公开读取、权限、迁移链和 Sitemap 专项 | 34 passed            |
| 独立真实 PostgreSQL 并发初始化                    | 1 passed             |
| 独立 PostgreSQL upgrade 到 0016                   | PASS                 |
| 独立 PostgreSQL 0016→0015→0016                    | PASS                 |
| Website Vitest                                    | 16 files / 155 tests |
| Admin Vitest                                      | 14 files / 88 tests  |
| Website/Admin typecheck                           | PASS / PASS          |
| Ruff 0.12.11                                      | PASS                 |
| Prettier                                          | PASS                 |
| API/Admin/Website Docker build                    | PASS                 |
| 1440/375 真实浏览器                               | PASS                 |

在“提交到 GitHub”这一轮又重新运行并通过 Website `155/155` 和 Admin `88/88`。当时宿主 Python 没有 pytest，当前 Codex 会话又无 Docker Engine 权限，因此 API 没有第三次重复执行；不得把这次阻塞写成新的 API PASS。

本次生成交接文档只改 Markdown，没有运行应用测试、构建、迁移或浏览器验收。应使用 `NOT_RUN`，不能复制上述历史数量冒充本轮测试。

## 13. 关键报告与证据

优先阅读：

1. `README.md`
2. `docs/content/demo-seo-metadata-r1-report.md`
3. `docs/content/demo-seo-six-pages-readback.json`
4. `docs/content/admin-cn-ux-r1-functional-closeout-report.md`
5. `docs/content/admin-cn-ux-r1-report.md`
6. `docs/content/demo-r2-1-review-closeout-report.md`
7. `docs/content/demo-r2-1-focused-closeout-report.md`
8. `docs/content/demo-r2-report.md`
9. `docs/content/demo-r2-maintenance-guide.md`
10. `docs/content/privacy-p1-content-version-report.md`
11. `docs/content/privacy-p1-admin-guide.md`
12. `docs/content/website-presentation-r1-report.md`
13. `docs/content/local-domain-preview-report.md`
14. `docs/content/phase3-7-3-products-sitepage-seo-report.md`
15. `docs/content/phase3-7-batch01-preview-display-fix-report.md`

当前最新本地证据包：

```text
Junhui-Demo-SEO-Metadata-R1-Evidence-20260910T234537+0800.zip
SHA-256: 5fa0a1412c299583dd4476fec638d913fb386c216056bebf2912e2f5dc050180
```

证据包和 `artifacts/` 保持本地，不默认提交 GitHub。

## 14. 不要重做的已验收事项

除非实际复现回归，不要重复开发：

- Batch01 COPY-V1 16 字段；
- 三张产品图尺寸回填和 Gallery；
- 本地域名、CA、hosts、VPN 精确直连和 loopback 443；
- 中文搜索、面包屑、同源语言导航；
- 空栏目后置与 Sitemap 候选规则；
- Products SitePage 和 8 组 SEO；
- Privacy P1 技术框架；
- 首页 14 模块、模块编辑器、完整布局预览和全站总览；
- Demo R2 的 90 条内容、48 项媒体和两段视频；
- Demo R2.1 五项视觉/维护收尾；
- 中文后台公共运营界面；
- 媒体具体使用位置；
- 角色权限中文编辑器；
- About/Contact/RFQ 六页 SSR SEO。

旧报告记录过的失败、工具缺失、截图伪影或 hydration 提示是历史证据。以当前代码和新复现为准，不应把已修复问题自动重新列为开放。

## 15. 仍然开放或需要单独授权的事项

- Privacy 最终中英文正文和人工流程尚未批准；
- 原始实例 Privacy 不得 Review/Publish/current 切换；
- RFQ 正式提交、真实附件、邮件发送仍未授权；
- Demo 资料尚未替换为全部可核验的正式资料；
- 真实参数、材料适用性、设备、产能、案例、客户、证书、专利、作者和市场事实需负责人确认；
- 数据保留/删除、请求台账、自动清理、NAS/工厂电脑备份流程没有实施；
- 腾讯云实际部署、地域接线、公共 DNS、Analytics、Search Console/Bing 等未授权；
- Lighthouse、真实用户 CWV、海外网络和 Safari/WebKit 不能沿用旧任务假定已通过；
- Phase 3.7 尚未整体完成，也没有生产上线许可。

## 16. 新对话开始时的最小核对

```powershell
Set-Location 'D:\Python_Project\New_Official_Website\junhui-global-website\.worktrees\phase-3.6'

git status --short --branch
git log -5 --oneline --decorate
git remote -v

powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\scripts\Junhui-Demo-R2-Launcher.ps1 -Action Status -NoOpen
```

若用户要看 Demo，再运行 Demo `Start`。若用户明确指定原批准内容预览，再运行 LocalPreview `Start`。启动后至少核对：

```text
127.0.0.1:443 正在监听
目标域名解析为 127.0.0.1
网站/Admin/API 返回真实本地内容
X-Junhui-Local-Preview 符合目标实例
X-Robots-Tag 仍为 noindex
/sitemap.xml 仍为 404
匿名管理 API 仍为 401
```

如果发现未提交文件，先识别来源并保留；禁止 `git reset --hard`、`git checkout --` 或整目录覆盖。若远端出现新提交，先比较差异，不自动 merge/rebase。

## 17. 推荐继续方式

1. 先启动并复核用户指定的目标实例；默认不把 Demo 操作理解为原始数据操作。
2. 对新需求先判断是“内容写入、代码改动、运行维护、只读审计还是 Git 提交”。
3. 内容写入必须读取当前 Revision，发现冲突先停止，不覆盖人工修改。
4. 后台保存继续走认证、权限、CSRF、Revision/Audit；生命周期变更走现有 Review/Publish/Route 服务。
5. Demo 可使用明确演示内容，但必须保持 Demo 标识和数据隔离。
6. 原始实例只使用负责人已批准事实，不能从 Demo 反向补齐。
7. 每轮记录真实运行结果；未测写 `NOT_RUN`，环境受阻写 `BLOCKED`。
8. 只有用户明确要求“提交到 GitHub”时才逐文件提交；证据、账号和私有材料继续留在本地。

## 18. 最终底线

本机页面返回 200、CMS 内部 published、测试通过或 SEO 完整，都不等于生产上线。任何生产部署、公共 DNS、Privacy 正式发布、真实 RFQ、邮件、Analytics、搜索平台提交或 Demo→正式数据迁移，都必须取得新的明确授权。
